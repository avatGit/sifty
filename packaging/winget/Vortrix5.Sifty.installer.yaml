# Installer manifest — see https://aka.ms/winget-manifest.installer.1.6.0.schema.json
# Sifty ships as a single-file console exe, so it installs as a "portable":
# winget drops sifty.exe and registers the `sifty` command alias.
PackageIdentifier: Vortrix5.Sifty
PackageVersion: 0.6.0
InstallerType: portable
Commands:
  - sifty
Installers:
  - Architecture: x64
    InstallerUrl: https://github.com/Vortrix5/sifty/releases/download/v0.6.0/sifty.exe
    InstallerSha256: REPLACE_WITH_SHA256
ManifestType: installer
ManifestVersion: 1.6.0
